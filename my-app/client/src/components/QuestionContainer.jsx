import React from "react";
// QuestionContainer.js	Handles transitions, animations, and layout for questions
// ❌ No state (just receives question)
// Renders the current question, manages UI transition

export default function QuestionContainer(){
    
}